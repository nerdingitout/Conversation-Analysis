console.log("Hello from app.js");

/*d3.csv("/Users/khalilfaraj/Desktop/STT/app/static/js/cases.csv").then(makeChart);

var country;
var value;
function makeChart(data) {
     country = data.map(function (d) { return d.province; });
     value = data.map(function (d) { return d.cumulative_cases; });
    console.log(country)

    // Bar chart
    new Chart(document.getElementById("myCanvas"), {
        type: 'bar',
        data: {
            labels: country,
            datasets: [
                {
                    label: "Population (millions)",
                    backgroundColor: ["#3e95cd", "#8e5ea2", "#3cba9f", "#e8c3b9", "#c45850"],
                    data: value
                }
            ]
        },
        options: {
            legend: { display: false },
            title: {
                display: true,
                text: 'Total Confirmed Cases of COVID in May'
            }
        }
    });
}*/

